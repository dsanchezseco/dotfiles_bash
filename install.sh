#!/bin/bash

#TODO: move the files aws_load_session.sh, artifactory_creds, bash_aliases and git_branch_info.sh to $HOME (hide files with a dot `.` before the name)
#TODO: concat concat2bashrc content to $HOME/.bashrc but controll that it can be done multiple times

#OPTIONAL TODO: create and populate artifactory_creds file
